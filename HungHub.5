local _c = "local P, R = game:GetService('Players').LocalPlayer, game:GetService('RunService'); local TS, HS, TP = game:GetService('TweenService'), game:GetService('HttpService'), game:GetService('TeleportService'); local RE = game:GetService('ReplicatedStorage'); local UI = Instance.new('ScreenGui', P:WaitForChild('PlayerGui')); UI.ResetOnSpawn, UI.Name = false, 'HungHub_RealKid_V24'; local ONE_PIECE_LOGO = 'rbxassetid://13441603501'; local mt = getrawmetatable and getrawmetatable(game); if mt and setreadonly then setreadonly(mt, false); local old = mt.__index; mt.__index = newcclosure(function(t, k) if tostring(t) == 'Humanoid' and (k == 'WalkSpeed' or k == 'JumpPower') then return k == 'WalkSpeed' and 16 or 50 end; return old(t, k) end); setreadonly(mt, true) end; local Main = Instance.new('Frame', UI); Main.Size, Main.Position, Main.BackgroundColor3, Main.Draggable, Main.Active = UDim2.new(0, 520, 0, 320), UDim2.new(0.1, 0, 0.2, 0), Color3.fromRGB(15, 15, 18), true, true; Instance.new('UIStroke', Main).Color, Instance.new('UICorner', Main).CornerRadius = Color3.fromRGB(45, 45, 50), UDim.new(0, 10); local TitleBar = Instance.new('Frame', Main); TitleBar.Size, TitleBar.Position, TitleBar.BackgroundColor3 = UDim2.new(1, 0, 0, 35), UDim2.new(0, 0, 0, 0), Color3.fromRGB(20, 20, 25); Instance.new('UICorner', TitleBar).CornerRadius = UDim.new(0, 10); local T = Instance.new('TextLabel', TitleBar); T.Size, T.Position, T.BackgroundTransparency, T.Text, T.TextColor3, T.Font, T.TextSize, T.TextXAlignment = UDim2.new(0, 300, 1, 0), UDim2.new(0, 15, 0, 0), 1, 'Hung Hub - Real Kid UI Engine v24', Color3.fromRGB(255, 255, 255), Enum.Font.GothamBold, 12, Enum.TextXAlignment.Left; local MinBtn = Instance.new('TextButton', TitleBar); MinBtn.Size, MinBtn.Position, MinBtn.Text, MinBtn.TextColor3, MinBtn.Font, MinBtn.BackgroundTransparency = UDim2.new(0, 25, 0, 25), UDim2.new(1, -60, 0, 5), '—', Color3.fromRGB(200, 200, 200), Enum.Font.GothamBold, 1; local CloseBtn = Instance.new('TextButton', TitleBar); CloseBtn.Size, CloseBtn.Position, CloseBtn.Text, CloseBtn.TextColor3, CloseBtn.Font, CloseBtn.BackgroundTransparency = UDim2.new(0, 25, 0, 25), UDim2.new(1, -30, 0, 5), '✕', Color3.fromRGB(255, 75, 75), Enum.Font.GothamBold, 1; local TabBar = Instance.new('Frame', Main); TabBar.Size, TabBar.Position, TabBar.BackgroundColor3 = UDim2.new(0, 130, 0, 275), UDim2.new(0, 5, 0, 40), Color3.fromRGB(18, 18, 22); Instance.new('UICorner', TabBar).CornerRadius = UDim.new(0, 6); local Container = Instance.new('Frame', Main); Container.Size, Container.Position, Container.BackgroundTransparency = UDim2.new(0, 375, 0, 275), UDim2.new(0, 140, 0, 40), 1; local Pages = {}; local function createPage(name) local p = Instance.new('ScrollingFrame', Container); p.Size, p.BackgroundTransparency, p.CanvasSize, p.ScrollBarThickness = UDim2.new(1, 0, 1, 0), 1, UDim2.new(0, 0, 2, 0), 2; p.Visible = false; Pages[name] = p; return p end; local PageFarm = createPage('Farm'); local PagePlayer = createPage('Player'); local PageEsp = createPage('Esp'); local PageMisc = createPage('Misc'); Pages['Farm'].Visible = true; local function showTab(name) for k, v in pairs(Pages) do v.Visible = (k == name) end end; local function tabBtn(txt, pos, pageName) local b = Instance.new('TextButton', TabBar); b.Size, b.Position, b.BackgroundColor3, b.TextColor3, b.Font, b.TextSize, b.Text = UDim2.new(0, 120, 0, 32), pos, Color3.fromRGB(25, 25, 30), Color3.fromRGB(180, 180, 180), Enum.Font.GothamBold, 11, '  ' .. txt; b.TextXAlignment = Enum.TextXAlignment.Left; Instance.new('UICorner', b).CornerRadius = UDim.new(0, 6); b.Activated:Connect(function() showTab(pageName) end); return b end; tabBtn('⚔️ Auto Farm', UDim2.new(0, 5, 0, 10), 'Farm'); tabBtn('👤 Bản Thân', UDim2.new(0, 5, 0, 47), 'Player'); tabBtn('👁️ ESP/Định Vị', UDim2.new(0, 5, 0, 84), 'Esp'); tabBtn('⚙️ Hệ Thống', UDim2.new(0, 5, 0, 121), 'Misc'); local TabLogo = Instance.new('ImageLabel', TabBar); TabLogo.Size, TabLogo.Position, TabLogo.Image, TabLogo.BackgroundTransparency = UDim2.new(0, 55, 0, 55), UDim2.new(0, 35, 0, 210), ONE_PIECE_LOGO, 1; local function cToggle(txt, pos, page, defaultState, callback) local state = defaultState or false; local f = Instance.new('Frame', page); f.Size, f.BackgroundTransparency, f.Position = UDim2.new(1, -10, 0, 35), 1, pos; local box = Instance.new('TextButton', f); box.Size, box.Position, box.Font, box.TextSize, box.Text = UDim2.new(0, 18, 0, 18), UDim2.new(0, 5, 0, 8), Enum.Font.GothamBold, 12, ''; box.BackgroundColor3 = state and Color3.fromRGB(0, 150, 255) or Color3.fromRGB(30, 30, 35); Instance.new('UICorner', box).CornerRadius = UDim.new(0, 4); local bs = Instance.new('UIStroke', box); bs.Color = state and Color3.fromRGB(255, 255, 255) or Color3.fromRGB(70, 70, 75); local lbl = Instance.new('TextLabel', f); lbl.Size, lbl.Position, lbl.BackgroundTransparency, lbl.Text, lbl.TextColor3, lbl.Font, lbl.TextSize, lbl.TextXAlignment = UDim2.new(1, -35, 1, 0), UDim2.new(0, 30, 0, 0), 1, txt, Color3.fromRGB(220, 220, 220), Enum.Font.GothamMedium, 11, Enum.TextXAlignment.Left; local function click() state = not state; box.BackgroundColor3 = state and Color3.fromRGB(0, 150, 255) or Color3.fromRGB(30, 30, 35); box.Text = state and '✓' or ''; box.TextColor3 = Color3.fromRGB(255, 255, 255); bs.Color = state and Color3.fromRGB(255, 255, 255) or Color3.fromRGB(70, 70, 75); callback(state) end; box.MouseButton1Click:Connect(click); box.TouchTap:Connect(click) end; local function cBox(txt, pos, page, ph) local b = Instance.new('TextBox', page); b.Size, b.Position, b.BackgroundColor3, b.TextColor3, b.Font, b.TextSize, b.Text, b.PlaceholderText = UDim2.new(0, 340, 0, 32), pos, Color3.fromRGB(22, 22, 26), Color3.fromRGB(255, 255, 255), Enum.Font.GothamBold, 11, txt, ph; Instance.new('UICorner', b).CornerRadius = UDim.new(0, 6); Instance.new('UIStroke', b).Color = Color3.fromRGB(50, 50, 55); return b end; local farm, fast, bring, m1, m2 = false, false, false, false, false; cToggle('Kích hoạt Auto Farm Level cốt truyện', UDim2.new(0, 0, 0, 5), PageFarm, false, function(s) farm = s end); cToggle('Fast Attack (Tấn công siêu tốc độ cao)', UDim2.new(0, 0, 0, 40), PageFarm, false, function(s) fast = s end); cToggle('Gom NPC Quái phạm vi rộng (Bring Mobs)', UDim2.new(0, 0, 0, 75), PageFarm, false, function(s) bring = s end); cToggle('Auto Farm Mastery vũ khí Võ cận chiến', UDim2.new(0, 0, 0, 110), PageFarm, false, function(s) m1 = s end); cToggle('Auto Farm Mastery trang bị Kiếm sĩ', UDim2.new(0, 0, 0, 145), PageFarm, false, function(s) m2 = s end); local fly, inf, noclip, stats = false, false, false, false; local InW = cBox('50', UDim2.new(0, 5, 0, 5), PagePlayer, 'Nhập WalkSpeed...'); local InJ = cBox('50', UDim2.new(0, 5, 0, 45), PagePlayer, 'Nhập JumpPower...'); cToggle('Kích hoạt chế độ Thuyền Bay (Fly)', UDim2.new(0, 0, 0, 85), PagePlayer, false, function(s) fly = s end); cToggle('Vô hạn nhảy không giới hạn (Infinite Geppo)', UDim2.new(0, 0, 0, 120), PagePlayer, false, function(s) inf = s end); cToggle('Xuyên tường chống kẹt địa hình (Noclip)', UDim2.new(0, 0, 0, 155), PagePlayer, false, function(s) noclip = s end); cToggle('Tự động cộng điểm Stats (Võ & Máu)', UDim2.new(0, 0, 0, 190), PagePlayer, false, function(s) stats = s end); local frt, sea, chest, rad = false, false, false, false; cToggle('Esp Berries (Hiển thị vị trí rương tiền)', UDim2.new(0, 0, 0, 5), PageEsp, false, function(s) chest = s end); cToggle('Esp Fruits (Hiển thị vị trí Trái Ác Quỷ)', UDim2.new(0, 0, 0, 40), PageEsp, false, function(s) frt = s end); cToggle('Esp Sea Beasts (Định vị quái biển sâu)', UDim2.new(0, 0, 0, 75), PageEsp, false, function(s) sea = s end); cToggle('Esp Raid Room (Khóa vị trí phòng Raid)', UDim2.new(0, 0, 0, 110), PageEsp, false, function(s) rad = s end); cToggle('Treo máy bảo vệ với lá chắn chống Ban ngầm', UDim2.new(0, 0, 0, 5), PageMisc, true, function(s) end); local BClr = Instance.new('TextButton', PageMisc); BClr.Size, BClr.Position, BClr.BackgroundColor3, BClr.TextColor3, BClr.Font, BClr.TextSize, BClr.Text = UDim2.new(0, 340, 0, 35), UDim2.new(0, 5, 0, 45), Color3.fromRGB(40, 20, 20), Color3.fromRGB(255, 75, 75), Enum.Font.GothamBold, 11, 'KÍCH HOẠT XOÁ SẠCH UI GAME / TĂNG FPS'; Instance.new('UICorner', BClr).CornerRadius = UDim.new(0, 6); Instance.new('UIStroke', BClr).Color = Color3.fromRGB(255, 75, 75); local BHop = Instance.new('TextButton', PageMisc); BHop.Size, BHop.Position, BHop.BackgroundColor3, BHop.TextColor3, BHop.Font, BHop.TextSize, BHop.Text = UDim2.new(0, 340, 0, 35), UDim2.new(0, 5, 0, 90), Color3.fromRGB(20, 35, 20), Color3.fromRGB(0, 255, 127), Enum.Font.GothamBold, 11, '🔄 BẤM ĐỂ CHUYỂN SERVER NGẪU NHIÊN'; Instance.new('UICorner', BHop).CornerRadius = UDim.new(0, 6); Instance.new('UIStroke', BHop).Color = Color3.fromRGB(0, 255, 127); local ToggleBtn = Instance.new('ImageButton', UI); ToggleBtn.Size, ToggleBtn.Position, ToggleBtn.BackgroundColor3, ToggleBtn.Image, ToggleBtn.Active, ToggleBtn.Draggable, ToggleBtn.Visible = UDim2.new(0, 55, 0, 55), UDim2.new(0.02, 0, 0.15, 0), Color3.fromRGB(15, 15, 15), ONE_PIECE_LOGO, true, true, false; Instance.new('UICorner', ToggleBtn).CornerRadius = UDim.new(1, 0); Instance.new('UIStroke', ToggleBtn).Color = Color3.fromRGB(255, 215, 0); MinBtn.MouseButton1Click:Connect(function() Main.Visible = false ToggleBtn.Visible = true end); CloseBtn.MouseButton1Click:Connect(function() UI:Destroy() end); ToggleBtn.MouseButton1Click:Connect(function() Main.Visible = true ToggleBtn.Visible = false end); _G.H_Spd, _G.H_Jmp = 50, 50; local bv, bg = nil, nil; InW:GetPropertyChangedSignal('Text'):Connect(function() _G.H_Spd = tonumber(InW.Text) or 16 end); InJ:GetPropertyChangedSignal('Text'):Connect(function() _G.H_Jmp = tonumber(InJ.Text) or 50 end); BClr.Activated:Connect(function() for _, g in pairs(P.PlayerGui:GetChildren()) do if g.Name ~= 'HungHub_RealKid_V24' and g:IsA('ScreenGui') then g:Destroy() end end end); BHop.Activated:Connect(function()local s = HS:JSONDecode(game:HttpGet('roblox.com' .. game.PlaceId .. '/servers/Public?sortOrder=Asc&limit=100')); for _, sv in pairs(s.data) do if sv.playing < sv.maxPlayers and sv.id ~= game.JobId then TP:TeleportToPlaceInstance(game.PlaceId, sv.id, P) break end end end); game:GetService('UserInputService').JumpRequest:Connect(function() if inf and P.Character and P.Character:FindFirstChildOfClass('Humanoid') then P.Character:FindFirstChildOfClass('Humanoid'):ChangeState(Enum.HumanoidStateType.Jumping) end end); local function eq(w) local bp = P:FindFirstChild('Backpack') if bp and P.Character then for _, t in pairs(bp:GetChildren()) do if t:IsA('Tool') and (string.find(string.lower(t.ToolTip or ''), w) or string.find(string.lower(t.Name), w)) then t.Parent = P.Character break end end end end; local function getQuestData() local lvl = P.Data:FindFirstChild('Level') and P.Data.Level.Value or 1; if lvl >= 1 and lvl < 15 then return 'BanditQuest1', 'Bandit', 1, CFrame.new(1059, 16, 1550) elseif lvl >= 15 and lvl < 30 then return 'JungleQuest', 'Monkey', 1, CFrame.new(-1598, 36, 154) elseif lvl >= 30 and lvl < 60 then return 'PirateVillageQuest', 'Pirate', 1, CFrame.new(-1184, 5, 3831) else return 'BanditQuest1', 'Bandit', 1, CFrame.new(1059, 16, 1550) end end; R.Heartbeat:Connect(function() local c = P.Character local hum = c and c:FindFirstChildOfClass('Humanoid') local root = c and c:FindFirstChild('HumanoidRootPart') if hum and root then local sp, jm = _G.H_Spd or 50, _G.H_Jmp or 50; hum.WalkSpeed = 16; if hum.JumpPower ~= jm then hum.UseJumpPower = true; hum.JumpPower = jm end; if noclip then for _, p in pairs(c:GetChildren()) do if p:IsA('BasePart') then p.CanCollide = false end end end; if stats then local r = RE:FindFirstChild('Remotes') and RE.Remotes:FindFirstChild('CommF') if r then r:InvokeServer('AddPoint', 'Melee', 3) r:InvokeServer('AddPoint', 'Defense', 3) end end; if chest then for _, v in pairs(game.Workspace:GetDescendants()) do if v:IsA('TouchTransmitter') and string.find(string.lower(v.Parent.Name), 'chest') then root.CFrame = v.Parent.CFrame; root.Velocity = Vector3.zero; break end end end; if bring then for _, v in pairs(game.Workspace:GetChildren()) do if v:IsA('Model') and v:FindFirstChildOfClass('Humanoid') and v ~= c then local vr = v:FindFirstChild('HumanoidRootPart') if vr and (vr.Position - root.Position).Magnitude < 320 then vr.CFrame = root.CFrame * CFrame.new(0, 0, -6) vr.Velocity = Vector3.zero end end end end; if frt then for _, v in pairs(game.Workspace:GetChildren()) do if v:IsA('Tool') and string.find(string.lower(v.Name), 'fruit') then root.CFrame = v.Handle.CFrame break end end end; if sea then for _, v in pairs(game.Workspace:GetChildren()) do if v:IsA('Model') and string.find(string.lower(v.Name), 'sea beast') and v:FindFirstChild('HumanoidRootPart') then root.CFrame = v.HumanoidRootPart.CFrame * CFrame.new(0, 26, 0) root.Velocity = Vector3.zero; if fast and c:FindFirstChildOfClass('Tool') then c:FindFirstChildOfClass('Tool'):Activate() end break end end end; if rad then local hE = false; for _, v in pairs(game.Workspace:GetChildren()) do if v:IsA('Model') and v:FindFirstChildOfClass('Humanoid') and v ~= c and v:FindFirstChild('HumanoidRootPart') and v:FindFirstChildOfClass('Humanoid').Health > 0 then local eR = v.HumanoidRootPart; if (eR.Position - root.Position).Magnitude < 350 then hE = true; root.CFrame = eR.CFrame * CFrame.new(0, 25, 0); root.Velocity = Vector3.zero; if c:FindFirstChildOfClass('Tool') then c:FindFirstChildOfClass('Tool'):Activate() end; local rS = game:GetService('ReplicatedStorage'); local comb = rS:FindFirstChild('Combat') or (rS:FindFirstChild('Remotes') and rS.Remotes:FindFirstChild('Validator')); if comb and comb:IsA('RemoteEvent') then comb:FireServer() end end end end; if not hE then root.Velocity = Vector3.zero end end; if m1 or m2 then if m1 then eq('melee') elseif m2 then eq('sword') end local e = nil; for _, v in pairs(game.Workspace:GetChildren()) do if v:IsA('Model') and v:FindFirstChildOfClass('Humanoid') and v ~= c and v:FindFirstChild('HumanoidRootPart') and v:FindFirstChildOfClass('Humanoid').Health > 0 then e = v; break end end; if e then root.CFrame = e.HumanoidRootPart.CFrame * CFrame.new(0, 8, 0); root.Velocity = Vector3.zero; if fast and c:FindFirstChildOfClass('Tool') then c:FindFirstChildOfClass('Tool'):Activate() end end end; if farm and not (m1 or m2 or rad) then eq('melee') local qName, eName, qId, npcPos = getQuestData() local hasQuest = P.PlayerGui.Main:FindFirstChild('Quest') and P.PlayerGui.Main.Quest.Visible; if not hasQuest then root.CFrame = npcPos; root.Velocity = Vector3.zero; local comm = RE:FindFirstChild('Remotes') and RE.Remotes:FindFirstChild('CommF') if comm then comm:InvokeServer('StartQuest', qName, qId) end else local targetMonster = nil; for _, v in pairs(game.Workspace:GetChildren()) do if v:IsA('Model') and string.find(v.Name, eName) and v:FindFirstChild('HumanoidRootPart') and v:FindFirstChildOfClass('Humanoid').Health > 0 then targetMonster = v break end end; if targetMonster then root.CFrame = targetMonster.HumanoidRootPart.CFrame * CFrame.new(0, 8, 0); root.Velocity = Vector3.zero; if fast and c:FindFirstChildOfClass('Tool') then c:FindFirstChildOfClass('Tool'):Activate() end else root.CFrame = npcPos * CFrame.new(0, 30, 0); root.Velocity = Vector3.zero end end end; if not fly then if hum.MoveDirection.Magnitude > 0 then root.CFrame = root.CFrame + (hum.MoveDirection * (sp - 16) * R.Heartbeat:DeltaTime()) end; if bv then bv:Destroy() bv = nil end; if bg then bg:Destroy() bg = nil end; root.CFrame = CFrame.new(root.Position) * CFrame.fromOrientation(0, select(2, root.CFrame:ToOrientation()), 0); root.RotVelocity = Vector3.zero else if not bv then bv = Instance.new('BodyVelocity', root); bv.MaxForce = Vector3.new(1e5, 1e5, 1e5) end; if not bg then bg = Instance.new('BodyGyro', root); bg.MaxTorque = Vector3.new(1e5, 1e5, 1e5); bg.D = 120 end; bg.CFrame = workspace.CurrentCamera.CFrame; local d = Vector3.zero; if hum.MoveDirection.Magnitude > 0 then d = (bg.CFrame.LookVector * hum.MoveDirection.Z + bg.CFrame.RightVector * hum.MoveDirection.X).Unit end; bv.Velocity = d * sp; root.Velocity = Vector3.zero; root.RotVelocity = Vector3.zero end end end)"loadstring(_c)()
